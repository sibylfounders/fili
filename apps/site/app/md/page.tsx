export default function MdHome() {
  return (
    <main className="mx-auto max-w-[820px] px-lg py-xl">
      <h1 className="text-3xl font-semibold text-text-primary">Doctrine</h1>
      <p className="text-text-secondary">
        Migration de DS-MD en cours — cette section rendra les règles UX/UI et les tokens
        depuis le contenu markdown, comme Audit, avec le même shell.
      </p>
    </main>
  );
}
