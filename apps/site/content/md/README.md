# Atelier

Source de vérité du projet. Le noyau (`core/`), les sujets et les inventaires vivent ici.

- Modifier ici, puis lancer `node tools/build.js`.
- Ne jamais reporter manuellement une correction dans `dist/` ou `public/`.
- Les assets éditoriaux vivent dans `content/`, pas dans l'atelier.

